<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.teamA.Haru.Controller.board.BoardDao">
    <select id="selectList" resultType="bvo">
    select * from board
    </select>
      <insert id="addBoard" parameterType="bvo">
     insert into board values(board_seq.nextVal,#{author},sysdate,#{contents},#{title})
    </insert>
</mapper>